# ObsStrawket

This repo is .NET client library for obs-websocket v5.

## Common Commands

```powershell
dotnet test
mise run lint
mise run fix
mise run generate-protocol
mise run update-protocol
```
